services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    env_file: .env.local
    ports:
      - "3000:3000"
    restart: unless-stopped

  # ─── Future Platform ──────────────────────────────────────────────────────
  # Uncomment when backend/database support is added (see Future Platform issues)
  #
  # backend:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.backend
  #   env_file: .env.local
  #   ports:
  #     - "3001:3001"
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #   restart: unless-stopped
  #
  # db:
  #   image: postgres:18
  #   env_file: .env.local
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U $${TEAMSHUFFLER_POSTGRES_USER} -d $${TEAMSHUFFLER_POSTGRES_DB}"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 10
  #     start_period: 10s
  #   restart: unless-stopped

# volumes:
#   postgres_data:
